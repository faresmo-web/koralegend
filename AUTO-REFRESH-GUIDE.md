# 🔄 دليل التحديث التلقائي - KoraLegend

## كيف يعمل النظام؟

النظام يتكون من جزئين:

### 1️⃣ الـ Scraper (جلب البيانات)
يقوم بجلب بيانات المباريات من 365scores API وحفظها في ملف `matches-data.js`

### 2️⃣ الموقع (عرض البيانات)
يقوم بعرض البيانات وتحديثها تلقائياً كل دقيقة

---

## 🚀 طريقة التشغيل

### الخطوة 1: تشغيل الـ Scraper في وضع Daemon

افتح Terminal وشغّل الأمر التالي:

```bash
node scraper.js --daemon --interval=1
```

**الشرح:**
- `--daemon`: يجعل السكريبت يعمل بشكل مستمر
- `--interval=1`: يحدث البيانات كل دقيقة واحدة

**ملاحظة:** يمكنك تغيير المدة:
- `--interval=2` → كل دقيقتين
- `--interval=5` → كل 5 دقائق

### الخطوة 2: افتح الموقع

افتح ملف `matches.html` في المتصفح

---

## ✨ المميزات

### في الـ Scraper:
- ✅ يجلب البيانات من 365scores تلقائياً
- ✅ يحدث ملف `matches-data.js` كل دقيقة
- ✅ يعطي أولوية للمباريات الجارية (Live)
- ✅ يعرض countdown للتحديث القادم

### في الموقع:
- ✅ يعيد تحميل الصفحة تلقائياً كل دقيقة
- ✅ يوقف التحديث عند إخفاء الصفحة (لتوفير الموارد)
- ✅ يعرض مؤشر "تحديث تلقائي" في أعلى الصفحة
- ✅ يعرض حالة المباريات بشكل صحيح:
  - 🔴 مباشر (Live) → للمباريات الجارية
  - ⚪ انتهت (Finished) → للمباريات المنتهية
  - 🔵 قادمة (Upcoming) → للمباريات القادمة

---

## 🎯 مثال على سير العمل

```
الوقت 00:00 → Scraper يجلب البيانات من 365scores
              ↓
              يحفظها في matches-data.js
              ↓
الوقت 00:01 → الموقع يعيد تحميل الصفحة
              ↓
              يعرض البيانات الجديدة
              ↓
الوقت 00:01 → Scraper يجلب البيانات مرة أخرى
              ↓
              ... وهكذا
```

---

## 🛠️ إعدادات متقدمة

### تغيير مدة التحديث في الموقع

افتح ملف `matches-data.js` وابحث عن:

```javascript
}, 60000); // 60000ms = 1 minute
```

غيّر القيمة:
- `30000` → نصف دقيقة
- `120000` → دقيقتين

### تغيير عدد المباريات المجلوبة

افتح ملف `scraper.js` وغيّر:

```javascript
const MAX_MATCHES_PER_DATE = 50;  // عدد المباريات لكل يوم
const MAX_DETAIL_SCRAPES = 40;    // عدد تفاصيل المباريات
```

---

## 📊 مراقبة الأداء

### في Terminal (Scraper):
```
✅ Scrape complete in 12.3s
⏱️  Next run in: 00:58
```

### في Browser Console (الموقع):
```
✅ Auto-refresh enabled (page will reload every 60 seconds)
🔄 Auto-refreshing page to get latest match data...
```

---

## ⚠️ ملاحظات مهمة

1. **لا تغلق Terminal** - يجب أن يبقى الـ Scraper يعمل في الخلفية
2. **الإنترنت مطلوب** - لجلب البيانات من 365scores
3. **التحديث التلقائي** - سيعيد تحميل الصفحة كل دقيقة (ستفقد أي فلاتر اخترتها)

---

## 🐛 حل المشاكل

### المشكلة: البيانات لا تتحدث
**الحل:**
1. تأكد أن الـ Scraper يعمل في Terminal
2. تحقق من اتصال الإنترنت
3. افتح Browser Console وابحث عن أخطاء

### المشكلة: الصفحة تعيد التحميل بسرعة كبيرة
**الحل:**
- غيّر قيمة `60000` في `matches-data.js` إلى رقم أكبر

### المشكلة: المباريات الجارية تظهر كأنها انتهت
**الحل:**
- تم إصلاح هذه المشكلة في آخر تحديث
- تأكد أن ملف `matches-data.js` محدّث

---

## 📝 الخلاصة

الآن لديك نظام تحديث تلقائي كامل:
- ✅ الـ Scraper يجلب البيانات كل دقيقة
- ✅ الموقع يعرض البيانات ويحدثها تلقائياً
- ✅ المباريات الجارية تظهر بشكل صحيح

**استمتع بمتابعة المباريات! ⚽🔥**
